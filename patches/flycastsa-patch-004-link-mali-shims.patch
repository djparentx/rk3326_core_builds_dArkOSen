--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -251,2 +251,8 @@
 	add_executable(${PROJECT_NAME} core/emulator.cpp)
+	if(FLYCAST_LINK_MALI_SHIMS)
+		target_sources(${PROJECT_NAME} PRIVATE
+			gbm_shim.c
+			wayland_egl_shim.c
+		)
+	endif()
 endif()